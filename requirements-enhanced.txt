# Enhanced Face Detection Requirements
# Install these for improved face detection accuracy and performance
# Note: dlib requires CMake and other build tools to be installed first

# For better accuracy (uses dlib internally with optimized defaults)
face-recognition>=1.3.0

# For real-time performance with high accuracy
mediapipe>=0.10.0

# Direct dlib support (optional, already included via face-recognition)
# dlib>=19.24.0
